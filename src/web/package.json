{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sync": "browser-sync start --proxy 'http://192.168.56.10:8080/' --files '.'",
    "sass": "node-sass --watch './src/scss/' --output-style 'compressed' --output './public/css/'",
    "start": "npm run sync & npm run sass"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.13",
    "node-sass": "^5.0.0",
    "sass": "^1.30.0"
  }
}
